-- displays the average temperature (Fahrenheit) by city ordered by
-- temperature (descending)
SELECT city, (SELECT AVG(value) FROM temperatures GROUP BY city
WHERE month BETWEEN 6 and 9) AS avg_temp
FROM temperatures
GROUP BY city
ORDER BY avg_temp DESC
LIMIT 3;
