#!/usr/bin/node

num = Number(process.argv[2]);
if (process.argv[2] === undefined) {
  console.log('Not a number');
} else if (num != num) {
  console.log('Not a number');
} else {
  console.log(num)
}
